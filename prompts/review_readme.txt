Given the following README, determine if this is specific to a project or
templated. Example of templated/generated READMEs include create-react-app,
create-next-app, etc. If it is templated, return 'templated'. Otherwise,
return 'specific'. No yapping– only respond with one word as your response.

{{url}}