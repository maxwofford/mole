Infer the user/project of a git repo provided by the user:

"{{url}}"

If this URL probably isn't a git repo or doesn't follow the naming conventions of one, return "NOT/FOUND"
Only reply with the format "USER/PROJECT", no details or json or other explaination. No yapping.